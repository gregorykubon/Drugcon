<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
package="com.example.grzegorz.drugcon">

<!-- To auto-complete the email text field in the login form with the user's emails -->
<uses-permission android:name="android.permission.GET_ACCOUNTS" />
<uses-permission android:name="android.permission.READ_PROFILE" />
<uses-permission android:name="android.permission.READ_CONTACTS" />
<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

<!-- To access Google+ APIs: -->
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="com.android.alarm.permission.SET_ALARM"></uses-permission>

<application
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/AppTheme">
    <activity
        android:name=".ui.activity.blank.Login"
        android:label="@string/app_name">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>

    </activity>
    <activity
        android:name=".ui.activity.blank.MenuAcc"
        android:label="@string/app_name">
    </activity>
    <activity
        android:name=".ui.activity.blank.Alarm"
        android:label="@string/app_name" />
    <activity
        android:name=".ui.activity.blank.List"
        android:label="@string/app_name" />
    <activity
        android:name=".ui.activity.blank.Search"
        android:label="@string/app_name" />
    <activity
        android:name=".ui.activity.blank.Show_List"
        android:label="@string/app_name" />
    <activity
        android:name=".ui.activity.blank.Change_Delete"
        android:label="@string/app_name" />
    <activity
        android:name=".ui.activity.blank.Change_Update"
        android:label="@string/app_name" />
    <activity
        android:name=".ui.activity.blank.Settings"
        android:label="@string/app_name" />
    <receiver android:name=".Notification_receiver">
        <intent-filter>
            <action android:name="android.intent.action.DISPLAY_NOTIFICATION"></action>
        </intent-filter>
    </receiver>
</application>

</manifest>